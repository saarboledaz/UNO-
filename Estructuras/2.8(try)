import java.util.*;

public class Main {

    public static void main(String[] argv) {
        Scanner e = new Scanner(System.in);
        short c = e.nextShort();
        for (int y = 0; y < c; y++) {

            short numero = e.nextShort();
            short tiempo = e.nextShort();
            short periodo = e.nextShort();
            List<Integer> A = new LinkedList<>();
            List<Integer> B = new LinkedList<>();
            for (int i = 0; i < numero; i++) {
                A.add(e.nextInt());
            }
            int i = 1;
            for (int j = numero; j > 0; j--) {
                i = (i + 8) % (numero--);
                if (A.size() <= (tiempo / periodo)) {
                    B.add(A.remove(i--));
                } else {
                    A.remove(i--);
                }
               
            }
            System.out.println(B);
        }
    }
}
